# This document has been created in order to documment all action required to deploy docker + kubernetese + ansible in localhost

# Create docker ssh image
```bash
~:$ docker build -t ssh_host:1.0.0 .
